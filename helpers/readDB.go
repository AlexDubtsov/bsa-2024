package helpers

func DBread() {

	// selectRecord := db.QueryRowContext(context.Background(), "select ID, AMOUNT, SPENT, CREATED from TRANSACTIONS where ID = ?", Single_Post.LoggedUserID)
	// err := selectRecord.Scan(&Single_Post.LoggedUserName)
	// if err != nil {
	// 	fmt.Println("Error scanning USERS 1:", err)
	// }
}
